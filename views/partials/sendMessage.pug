div(ng-controller='sendMessageCtrl')
  form(ng-submit='processForm()')
    div#name-group
      label Message
      input(
        type='text'
        ng-model="formData.content"
      )
    button(
      type='submit'
    )
      span Submit
  div#info

