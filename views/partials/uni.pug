div()
    div.container
        h1 Search by Uni
    div.container
        form(ng-submit='processForm()')
                    div.form-group
                        select(ng-model="formData.select" ng-options="uni['code'] as uni['name'] for uni in universities").form-control
                        br
                        div.row
                            div.col-3
                                div(ng-click="DateToggle()").btn.btn-success#date Pick a date
                            div.col-3
                                span From: 
                                input(ng-model="formData.dateFrom" type="date" disabled id="from")
                            div.col-3
                                span To: 
                                input(ng-model="formData.dateTo" type="date" disabled id="to")
                        br
                        button(
                            type='submit'
                            ).btn.btn-lg.btn-primary.btn-block Search

        div(id="results" class="uni9-div-pres-res").container
            div(ng-repeat="course in courses" ng-click="ViewCourse(course['_id'])" class="uni9-div-res")
                div.row
                    div.col-5
                        h5 {{course['sport']}}
                    div.col-5
                    div.col-2
                        p {{course['university']['code']}}
                div.row
                    div.col-5
                        p {{course['category']['name']}}
                    div.col-2
                    div.col-5
                        p.uni9-pres-res-right {{course['times']}}{{course['day']}} {{course['time']}}