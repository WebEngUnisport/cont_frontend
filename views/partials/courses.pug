div()
    div.container
        h1 Search by courses
    div.container
        form(action="ng-submit='processForm()'")
                    div.form-group
                        select.form-control
                            option {{categories}}